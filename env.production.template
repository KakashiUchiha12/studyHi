# Production Environment Configuration Template
# Copy this file to .env.production and fill in your actual values

# Node Environment
NODE_ENV=production

# Application URL (replace with your actual deployment URL)
NEXTAUTH_URL=http://your-server-ip.nip.io

# NextAuth Secret (IMPORTANT: Generate a secure secret for production)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secure-nextauth-secret-here

# Database Configuration (replace with your actual database credentials)
DATABASE_URL=mysql://username:password@host:3306/database_name

# Pusher Configuration (for real-time features - get from pusher.com)
PUSHER_APP_ID="your_pusher_app_id"
PUSHER_KEY="your_pusher_key"
PUSHER_SECRET="your_pusher_secret"
PUSHER_CLUSTER="your_pusher_cluster"

# Google OAuth Configuration (get from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Instructions:
# 1. Copy this file: cp env.production.template .env.production
# 2. Replace all placeholder values with your actual credentials
# 3. Ensure .env.production is in .gitignore (it should be by default)
# 4. Never commit .env.production to version control

# For the actual production values, please refer to the deployment documentation
# or contact the repository administrator

